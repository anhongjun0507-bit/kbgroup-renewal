================================================================
Phase 6 — KB GROUP 리뉴얼 사이트 잔여 수정 지시서 (Claude Code용)
대상: https://kbgroup-renewal.vercel.app/
생성일: 2026-05-16T10:06:15.543Z
================================================================

본 문서 구성
  PART 1. 상세 버전 (A ~ K 전수, 원인 + 구체 코드값 포함)
  PART 2. 요약 체크리스트 (P0~P3별 한 줄 요약)
  PART 3. 우선순위 테이블
  PART 4. 토큰/유틸 전체 (globals.css 그대로 붙여넣기용)

전제: Phase 1~5 결과 검증 결과, 토큰·푸터·일부 라우팅은 반영되었으나
      시각·인터랙션 핵심 요소가 다수 미반영. 아래 항목을 Phase 6로 처리.

================================================================
PART 1. 상세 버전 (A ~ K)
================================================================

────────────────────────────────────────
A. CRITICAL — 사이트 전반 영향 버그 (P0)
────────────────────────────────────────

[A-1] 큰 번호 "01 02 03 04 05" 수직 분리 렌더링 버그 (about, business/*)
- 현상: about "다섯 가지 차이", business/facility "WHY US" 등에서
  큰 워터마크 숫자가 "0" 위, "1" 아래 2줄로 끊겨 렌더링됨.
- 원인: 좌측 컬럼 width가 너무 좁고 font-size(64~80px) 대비 컨테이너가 작아
  자동 줄바꿈 발생. 또는 grid item에 min-width:0 누락.
- 수정:
  .number-display {
    width: auto;
    min-width: 5ch;
    white-space: nowrap;
    overflow: visible;
    line-height: 1;
    font-variant-numeric: tabular-nums;
    font-size: clamp(48px, 6vw, 88px);
    letter-spacing: -0.02em;
  }
  /* 부모 grid */
  .why-us-row { display:grid; grid-template-columns: minmax(160px, 200px) 1fr;
                align-items:center; gap:32px; }
  @media (max-width: 768px) {
    .why-us-row { grid-template-columns: 1fr; }
  }

[A-2] BY THE NUMBERS / 통계 카운트 잔존 회색 + 줄바꿈 버그
- 현상1 (home·about·cases·licenses): 카운트업 종료 후 일부 숫자가 회색으로 남아
  비활성처럼 보임. 일관성 없음(8+/0+/0+/0+ → 일부만 카운트업).
- 현상2 (about 통계 배너): "11,997+"가 "11,99" + 다음줄 "7"로 줄바꿈.
- 수정:
  • CountUp 컴포넌트:
    - useEffect cleanup 누락 점검
    - IntersectionObserver threshold 0.3 → 0.1, rootMargin '-10% 0px'
    - 종료 후 setState로 final value 명확히 고정
  • 종료 색은 isFinal 상태로 강제:
    .stat-number { color: var(--navy-900); white-space: nowrap;
                   font-variant-numeric: tabular-nums; }
    .stat-number[data-animating="true"] { color: var(--gray-400); }
  • 숫자 셀 가로 확보:
    .stat-cell { min-width: clamp(120px, 14vw, 200px); white-space: nowrap; }
    .stat-suffix { margin-left: 4px; display: inline-block; }
  • 4컬럼 grid 강제:
    .stats-grid { display:grid; grid-template-columns: repeat(4, minmax(0,1fr)); gap:24px; }

[A-3] 다크 배경 위 텍스트 대비 부족 (cases·licenses 상단 배너)
- 현상: cases 상단 "전국 8개 단지에서…" 헤드라인이 medium gray로 거의 안 보임.
  licenses "1,575명의 전문 인력이…" 동일.
- 수정 (토큰 추가):
  :root {
    --on-dark-primary: #FFFFFF;
    --on-dark-secondary: rgba(255,255,255,0.78);
    --on-dark-muted: rgba(255,255,255,0.55);
  }
  .section-dark { background: var(--navy-900); color: var(--on-dark-secondary); }
  .section-dark h1,.section-dark h2,.section-dark h3 { color: var(--on-dark-primary); }
  .section-dark .muted { color: var(--on-dark-muted); }
  .section-dark a       { color: var(--accent-300); }
- 적용처: cases hero, licenses hero, 그 외 navy 배경 섹션 전수 점검.

[A-4] 헤더 sticky + blur 미적용
- 현상: position: sticky / backdrop-filter 모두 미적용. Phase 2 스펙 누락.
- 수정 CSS:
  header.site-header {
    position: sticky; top: 0; z-index: 50;
    background: rgba(255,255,255,0.72);
    backdrop-filter: saturate(140%) blur(14px);
    -webkit-backdrop-filter: saturate(140%) blur(14px);
    border-bottom: 1px solid rgba(15,23,42,0.06);
    transition: background var(--ease) .2s, border-color var(--ease) .2s;
  }
  header.site-header[data-scrolled="true"] {
    background: rgba(255,255,255,0.88);
    box-shadow: var(--shadow-card);
  }
  header.site-header[data-over-dark="true"] {
    background: rgba(14,23,51,0.65);
    border-bottom-color: rgba(255,255,255,0.06);
  }
  header.site-header[data-over-dark="true"] a { color: #fff; }
- 수정 JS:
  useEffect(() => {
    const el = document.querySelector('header');
    const onScroll = () => {
      el?.setAttribute('data-scrolled', String(window.scrollY > 8));
      // 다크 hero 위 여부는 IntersectionObserver로 별도 처리
    };
    window.addEventListener('scroll', onScroll, { passive: true });
    return () => window.removeEventListener('scroll', onScroll);
  }, []);

[A-5] /cases/[slug] 404, /contact 404
- 수정:
  • app/cases/[slug]/page.tsx 신설 — Phase 5 케이스 상세 스켈레톤
    export async function generateStaticParams() {
      return CASES.map(c => ({ slug: c.slug }));
    }
    export default function Page({ params }) {
      const data = CASES.find(c => c.slug === params.slug);
      if (!data) return notFound();
      return <CaseDetail data={data} />;
    }
  • app/contact/page.tsx 신설 — ContactForm + 지도/연락처 3분할
  • 카드 ↔ 상세 라우트 컨벤션 통일 (slug 한 가지로 고정)
  • next.config.js redirects 점검

[A-6] 본문 그레이 토큰 미반영
- 현상: body color 여전히 #5A6472. Phase 1 명세는 #3F4754.
- 수정 (globals.css):
  :root { --text-body: #3F4754; }
  body  { color: var(--text-body); line-height: 1.75;
          font-feature-settings: "ss10","tnum";
          letter-spacing: -0.01em; }
- 컴포넌트 하드코딩 color:#5A6472 전수 검색 후 토큰으로 치환.

────────────────────────────────────────
B. HOME (/) — 미반영 시각 요소 (P1)
────────────────────────────────────────

[B-1] Hero 배경 mesh gradient + 빌딩 SVG
- hero section background:
  background:
    radial-gradient(at 20% 30%, rgba(67,86,168,0.55), transparent 50%),
    radial-gradient(at 80% 70%, rgba(230,57,80,0.18), transparent 45%),
    radial-gradient(at 50% 100%, rgba(14,23,51,0) 0%, rgba(14,23,51,1) 100%),
    linear-gradient(180deg, #0E1733 0%, #15203F 100%);
- 우측 50%에 <HeroBuildingsSvg /> (도시 스카이라인 4~6동, stroke only)
  opacity: 0.12; mix-blend-mode: screen; position:absolute; right:0; bottom:0;
- 창문 일부에 yellow dot 5~10개, keyframe twinkle 3~6s, opacity 0.3~1, random delay.
- 격자 패턴 SVG <pattern> 별도 레이어, opacity 0.04.

[B-2] Hero 통계 floating glass card
- 11년 / 12,000+ / 85+ 통계를 hero 좌측 텍스트 아래 인라인이 아닌,
  hero 하단을 -60px 가리는 글래스 카드로 추출.
  .hero-stats-card {
    position: absolute; bottom: -60px; left: 50%;
    transform: translateX(-50%);
    background: rgba(255,255,255,0.06);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 16px;
    padding: 32px 48px;
    display: flex; gap: 64px;
    box-shadow: 0 24px 64px rgba(0,0,0,0.3);
  }
- 다음 섹션 padding-top을 60px 증가시켜 카드 침범 보정.

[B-3] BUSINESS 카드 16:9 비주얼 영역 + SVG 라인 일러스트
- 5개 카드 각 상단에 aspect-ratio: 16/9 슬롯.
- SVG 라인 일러스트 5종:
    facility    = 공구 + 계측기
    sanitation  = 물방울 + 브러시
    security    = 실드 + CCTV
    construction= 크레인 + 도면
    others      = 박스 + 트럭
- 카드별 액센트 컬러 (좌상단 색띠):
    facility=#3B82F6, sanitation=#10B981, security=#F59E0B,
    construction=#8B5CF6, others=#64748B
- hover: 워터마크 숫자(01~05)가 카드 액센트 컬러 12%로 변경,
         "자세히 보기 →" 화살표 16px 우측 이동.
- 4컬럼 grid + 5번째 "기타"는 별도 행 wide(2 col)로 차별화.

[B-4] CASES placeholder 업그레이드
- 현상: 첫 카드에 LH 배지 누락, aspect 16:9, 호버 인터랙션 부족.
- 수정:
  • 카드 컴포넌트 통일:
    - 좌상단 발주처 배지 (LH=accent 핑크, 민간=navy-700)
    - 우하단 위치 마커 SVG + 지역명
    - 중앙 단지 로고 SVG placeholder (KB 단순화 또는 이니셜 모노그램)
  • aspect-ratio 4:5로 변경
  • hover: scale(1.03) ken-burns 8s ease-in-out alternate
           오버레이 rgba(14,23,51,0.45) → 0.25, 트랜지션 600ms ease
  • 데이터 누락 점검: 모든 카드에 발주처/연도/세대수 필수 필드 채우기.

[B-5] CONTACT 인라인 폼 (Phase 2/5 선언, 미반영)
- 현상: 062 전화 + 이메일 문의 버튼만, 실제 폼 부재.
- 수정 — <ContactForm> 컴포넌트 신설:
  필드:
    - 회사명 (text)
    - 담당자 (text)
    - 연락처 (tel)
    - 이메일 (email)
    - 단지 규모 — 세대수 (number)
    - 문의 유형 (select: 시설관리/위생청소/경비보안/시행건설/기타)
    - 상담 희망일 (date)
    - 문의 내용 (textarea)
    - 개인정보 동의 (checkbox 필수)
  UI:
    - 2 column grid (모바일 1열)
    - 라벨 위, 입력 하단 border-bottom 1px (focus 시 accent-500 2px)
  제출:
    - app/api/contact/route.ts 에서 Resend 또는 SendGrid 호출
    - Slack webhook 옵션
  성공:
    - inline toast 또는 모달 "문의가 접수되었습니다 · 평일 24시간 내 회신"
  배치:
    - home, business/*, cases, careers, licenses, about/* 등 9개 페이지 하단 노출.

────────────────────────────────────────
C. ABOUT — 인터랙션/구조 (P1)
────────────────────────────────────────

[C-1] /about 탭 Pill 디자인 미적용
- 현상: 그냥 텍스트 링크. active 표시 없음.
- 수정:
  .tab-pill {
    display:inline-flex; align-items:center; gap:6px;
    padding: 10px 18px;
    border-radius: 999px;
    border: 1px solid var(--gray-200);
    color: var(--gray-700);
    font-weight: 500;
    transition: all var(--ease) .18s;
  }
  .tab-pill:hover { border-color: var(--navy-700); color: var(--navy-700); }
  .tab-pill[aria-current="page"] {
    background: var(--navy-900); color:#fff; border-color: var(--navy-900);
  }

[C-2] "세 가지 가치" 카드 평면화 문제
- 수정:
  • 카드 상단 아이콘 원형 배지 48px (배경 accent-100, 아이콘 accent-500 stroke 1.5)
  • hover: transform: translateY(-4px); box-shadow: var(--shadow-card);
  • 카드 내 H3 (20/28) + body (16/26) + 하단 verbose link 'Read more →'

[C-3] "다섯 가지 차이" sticky 스크롤 미작동
- 수정:
  • 2분할 레이아웃: 좌측(인덱스, sticky top:96px), 우측(콘텐츠 카드 5장)
  • IntersectionObserver로 좌측 인덱스 active 표시
    rootMargin: '-40% 0px -40% 0px', threshold: 0
  • 각 카드 진입 시 fade-in 16px translateY → 0

[C-4] /about/history sticky 타임라인 미적용
- 수정:
  • 좌측 연도(sticky top:120px) + 우측 카드 리스트 (각 카드 24px gap)
  • 연도 변경 시 좌측 큰 연도 텍스트 fade-in 200ms
  • 좌측 폭 minmax(160px, 220px), 우측 1fr

[C-5] /about/location 카카오 지도 placeholder만 표시
- 수정:
  • Kakao Map SDK 연동 (env: NEXT_PUBLIC_KAKAO_MAP_KEY)
  • <script src='//dapi.kakao.com/v2/maps/sdk.js?autoload=false&appkey=...'> 동적 로드
  • 본사 좌표 마커, info window
  • 지도 하단 3분할 카드: 본사 / 지사 / 연락처
  • 카드 아이콘: MapPin / Building2 / Phone (lucide)

────────────────────────────────────────
D. BUSINESS — 구조/지표 (P1)
────────────────────────────────────────

[D-1] /business 가 home과 동일, alternating 레이아웃 미적용
- 수정:
  • 각 사업 영역마다 좌/우 교차 레이아웃
    section.biz-row:nth-child(even) { flex-direction: row-reverse; }
    .biz-row { display:flex; gap: 64px; align-items: center; }
  • 좌측: 16:9 비주얼(SVG 일러스트 + 액센트 색띠)
  • 우측: 카테고리 라벨 → H2 → body 2~3 줄 → 3 metrics → CTA
  • 모바일 1열, 그림 위 → 텍스트 아래

[D-2] 3대 지표(metrics) 누락
- 수정:
  • 각 사업영역 상단에 3개 숫자 메트릭
    예: 관리 세대 11,997+ / 단지 수 87 / 평균 만족도 4.8
  • 컴포넌트 <MetricsRow items={[{label,value,suffix}]} />
  • 숫자: display-2 (48/56), label: caption (13/20, gray-500)
  • 모바일 가로 스크롤 허용 (overflow-x: auto; scroll-snap)

[D-3] business/facility 의 PROCESS 4단계 외에 갤러리 + FAQ accordion 누락
- 수정:
  • 갤러리: aspect 4:3 카드 6장, 좌→우 grid, hover 시 caption fade-in
  • FAQ accordion: shadcn Accordion, 5~7 QnA, animateIn smooth
  • PROCESS 4단계 카드 사이 점선 connector (모바일에서는 세로 connector)

────────────────────────────────────────
E. CASES — 지도/필터/상세 (P1)
────────────────────────────────────────

[E-1] SVG 한국 지도 누락
- 수정:
  • 헤더 영역에 단순화된 한국 지도 SVG (svg-korea-simple)
  • 시공 지역에 dot (accent-500, r=6) 표시, hover 시 툴팁 (지역명 + 단지명)
  • 클릭 시 해당 지역 필터로 카드 목록 스크롤
  • 지도 색: navy-900 base, dot accent-500, hover dot scale 1.3

[E-2] Sticky 필터 바
- 수정:
  .case-filter-bar {
    position: sticky; top: 72px;
    background: rgba(255,255,255,0.9);
    backdrop-filter: blur(10px);
    z-index: 30;
    padding: 12px 0;
    border-bottom: 1px solid var(--gray-200);
  }
  - chips: 전체 / LH / 민간 / 연도별
  - 선택된 chip: bg navy-900, color #fff

[E-3] /cases/[slug] 상세 스켈레톤
- 페이지 구조:
    1) Hero 이미지 (full-bleed, 16:9 또는 21:9)
    2) 프로젝트 메타 (연도 / 위치 / 발주처 / 규모 - 4 컬럼 grid)
    3) 본문 (2 column: 좌 30% 인덱스 sticky, 우 70% prose)
    4) 갤러리 (mason grid 또는 4 columns)
    5) 다음/이전 케이스 (full-bleed nav 카드)
- generateStaticParams로 모든 슬러그 프리렌더.

[E-4] 카드 호버 인터랙션
- 이미지 transform: scale(1.04); transition: 600ms ease
- 어두운 오버레이 opacity 0 → 0.6
- 타이틀 underline-grow (좌→우 reveal, 300ms)
- 카드 카드 사이 그림자 deepen: shadow-card → shadow-cta

────────────────────────────────────────
F. LICENSES — 데이터 시각화 (P1)
────────────────────────────────────────

[F-1] 통계 대시보드 헤더 누락
- 수정:
  3 KPI 카드 가로 배치:
    - 보유 면허 N건
    - 카테고리 N개
    - 최신 갱신 YYYY년
  스타일: 카드 padding 32px, navy-700 left-border 4px, KPI 숫자 display-2.

[F-2] 도넛 차트 미적용
- 수정:
  • Recharts <PieChart> 또는 순수 SVG
  • 카테고리별 분포 (시공/감리/엔지니어링/안전 등)
  • 중앙 라벨: 총 면허 수, 하단 legend
  • 색 팔레트: accent + navy 계열 (5단계)
  • 모바일에서는 차트 위/legend 아래로 자동 stack

[F-3] 라이트박스 미적용
- 수정:
  • yet-another-react-lightbox 또는 자체 모달
  • 면허증 썸네일 클릭 시 라이트박스 오픈
  • 키보드 ←/→ 이동, ESC 닫기, 핀치줌 (모바일)
  • alt 텍스트 필수 (면허명 + 발급기관)

[F-4] 어두운 섹션 본문 가독성
- A-3 토큰 적용 (--on-dark-secondary, --on-dark-muted)
- licenses hero "1,575명의 전문 인력이…" 흰색 primary로 강제.

────────────────────────────────────────
G. CAREERS — 모달/리스트 (P1)
────────────────────────────────────────

[G-1] BENEFITS 8개 그리드 (현재 6개)
- 4×2 grid, 각 셀: 아이콘(40px) + H4 타이틀 + 1줄 설명
- 아이콘 후보 (lucide): Heart, Coffee, Plane, BookOpen, Trophy, Shield, Users, Smile
- 카드 hover: 배경 gray-50 → white, 그림자 deepen

[G-2] 인재풀(Talent Pool) 모달 누락
- 수정:
  • "상시 지원" CTA 클릭 → shadcn Dialog
  • 필드: 이름 / 연락처 / 이메일 / 희망 분야(select) / 한 줄 자기소개 / 이력서 파일 업로드
  • 백드롭 blur, 모달 max-width 560px
  • 제출 시 /api/talent-pool POST (S3 또는 Resend 첨부)

[G-3] 채용 공고 placeholder → 실제 리스트 컴포넌트
- 카드 한 줄 row:
    좌: 직무 (H4) + 부서 + 위치 chip
    우: 마감일 + "지원하기" CTA
- 카드 hover: 배경 gray-50, 좌측 4px navy-700 border 등장

[G-4] Hero 카피 위계
- display-1 (64/72) + body-lg (18/30) + Primary CTA "인재 지원" + Ghost "문화 보기"
- Hero 우측 30% 영역에 팀 일러스트 SVG (라인, accent 컬러)

────────────────────────────────────────
H. NOTICES — 목록/필터/상세 (P1)
────────────────────────────────────────

[H-1] 여전히 COMING SOON placeholder
- 수정:
  • 상단 카테고리 필터 chip: 전체 / 공지 / 보도 / 채용
  • 리스트 row: 카테고리 chip / 제목 / 날짜 (우측 정렬)
  • hover: 배경 gray-50, 제목 underline reveal
  • 빈 상태: 일러스트 (SVG) + 카피 "아직 등록된 공지가 없습니다"
  • /notices/[slug] 상세 스켈레톤: 메타 (카테고리, 날짜) + 본문 prose
  • 페이지네이션 (10건/페이지) 또는 무한 스크롤

────────────────────────────────────────
I. 디자인 일관성 (P2)
────────────────────────────────────────

[I-1] 섹션 패딩 통일
  --section-py-desktop: 120px;
  --section-py-tablet : 80px;
  --section-py-mobile : 56px;

[I-2] 타이포 스케일 통일
  display-1: 64/72, letter-spacing -0.02em
  display-2: 48/56, letter-spacing -0.02em
  h1: 40/48 / h2: 32/40 / h3: 24/32
  body-lg: 18/30 / body: 16/28 / caption: 13/20

[I-3] 버튼 사이즈 토큰
  --btn-h-sm: 36px; --btn-h-md: 44px; --btn-h-lg: 52px;

[I-4] 아이콘 stroke-width 1.5 통일 (lucide-react)

[I-5] 포커스 링 통일
  :focus-visible { outline: 2px solid var(--accent-500); outline-offset: 2px; }

[I-6] 이미지 모서리 토큰
  카드 이미지 border-radius: var(--radius-md) (12px)

[I-7] 모션 토큰 통일
  --ease: cubic-bezier(.22,.61,.36,1);
  --dur-fast: 160ms; --dur-base: 220ms; --dur-slow: 420ms;

────────────────────────────────────────
J. 우선순위
────────────────────────────────────────

P0 (즉시)         : A-1, A-2, A-3, A-4, A-5, A-6
P1 (이번 스프린트): B-1~B-5, C-1~C-5, D-1~D-3, E-1~E-4, F-1~F-4, G-1~G-4, H-1
P2 (다음 스프린트): I-1~I-7
P3 (백로그)       : Storybook 디자인 시스템 문서화, 케이스 CMS 연동, i18n

────────────────────────────────────────
K. 추가 토큰 / 유틸 (globals.css 통째로 붙여넣기용)
────────────────────────────────────────

:root {
  /* color - navy */
  --navy-900: #0B1A33;
  --navy-800: #0E1F3A;
  --navy-700: #16315C;
  --navy-500: #2A4E8F;
  --navy-100: #E7ECF5;

  /* color - accent */
  --accent-500: #C9A24B;
  --accent-300: #E3C57A;
  --accent-100: #F6EBC9;

  /* color - gray */
  --gray-50:  #F7F8FA;
  --gray-100: #EEF1F5;
  --gray-200: #E1E6ED;
  --gray-300: #C9D0DA;
  --gray-400: #9AA3B2;
  --gray-500: #6B7383;
  --gray-600: #3F4754;
  --gray-700: #2A323F;
  --gray-900: #1B2230;
  --text-body: #3F4754;

  /* on-dark */
  --on-dark-primary: #FFFFFF;
  --on-dark-secondary: rgba(255,255,255,0.78);
  --on-dark-muted: rgba(255,255,255,0.55);

  /* radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 20px;

  /* shadow */
  --shadow-card: 0 4px 16px rgba(11, 26, 51, 0.06);
  --shadow-cta : 0 8px 24px rgba(201, 162, 75, 0.28);

  /* motion */
  --ease: cubic-bezier(.22,.61,.36,1);
  --dur-fast: 160ms;
  --dur-base: 220ms;
  --dur-slow: 420ms;

  /* spacing */
  --section-py-desktop: 120px;
  --section-py-tablet : 80px;
  --section-py-mobile : 56px;

  /* button */
  --btn-h-sm: 36px;
  --btn-h-md: 44px;
  --btn-h-lg: 52px;
}

body {
  color: var(--text-body);
  line-height: 1.75;
  font-feature-settings: "ss10","tnum";
  letter-spacing: -0.01em;
}

/* utils */
.section        { padding-block: var(--section-py-desktop); }
@media (max-width: 1024px) { .section { padding-block: var(--section-py-tablet); } }
@media (max-width: 640px)  { .section { padding-block: var(--section-py-mobile); } }

.container      { max-width: 1200px; margin-inline: auto; padding-inline: 24px; }
.tabular        { font-variant-numeric: tabular-nums; }
.nowrap         { white-space: nowrap; }
.balance        { text-wrap: balance; }

.section-dark   { background: var(--navy-900); color: var(--on-dark-secondary); }
.section-dark h1,.section-dark h2,.section-dark h3 { color: var(--on-dark-primary); }
.section-dark .muted { color: var(--on-dark-muted); }
.section-dark a       { color: var(--accent-300); }

:focus-visible {
  outline: 2px solid var(--accent-500);
  outline-offset: 2px;
}

================================================================
PART 2. 요약 체크리스트 (한 줄 요약)
================================================================

[P0]
[ ] A-1  숫자 워터마크 white-space:nowrap + grid minmax(160px,200px)
[ ] A-2  CountUp final state + stat-cell min-width + 4컬럼 grid
[ ] A-3  --on-dark-* 토큰 도입, .section-dark 클래스 적용
[ ] A-4  header sticky + backdrop-filter blur + data-scrolled 토글
[ ] A-5  /cases/[slug], /contact 페이지 생성 + generateStaticParams
[ ] A-6  body color #5A6472 → #3F4754 토큰 전수 치환

[P1 - Home]
[ ] B-1  Hero mesh gradient + 빌딩 SVG + twinkle dot
[ ] B-2  Floating glass stat card (hero 하단 -60px)
[ ] B-3  BUSINESS 16:9 비주얼 + SVG 라인 일러스트 5종 + 액센트 색띠
[ ] B-4  CASES 카드 LH 배지, aspect 4:5, ken-burns hover
[ ] B-5  ContactForm 9필드 + Resend/Slack + 9페이지 하단 노출

[P1 - About]
[ ] C-1  탭 pill 디자인 + aria-current
[ ] C-2  '세 가지 가치' 아이콘 배지 + hover lift
[ ] C-3  '다섯 가지 차이' sticky 좌측 인덱스 + IO
[ ] C-4  history sticky 좌측 연도 타임라인
[ ] C-5  Kakao Map SDK + 본사/지사/연락처 3분할

[P1 - Business]
[ ] D-1  alternating 레이아웃 (row-reverse)
[ ] D-2  3 metrics row
[ ] D-3  갤러리 + FAQ accordion + PROCESS connector

[P1 - Cases]
[ ] E-1  SVG 한국 지도 + dot 마커 + 툴팁
[ ] E-2  sticky 필터 바
[ ] E-3  /cases/[slug] 5섹션 스켈레톤
[ ] E-4  카드 hover ken-burns + 오버레이

[P1 - Licenses]
[ ] F-1  3 KPI 대시보드 헤더
[ ] F-2  도넛 차트 (Recharts/SVG)
[ ] F-3  라이트박스
[ ] F-4  다크 섹션 토큰 적용

[P1 - Careers]
[ ] G-1  BENEFITS 4×2 = 8개
[ ] G-2  인재풀 모달 + 파일 업로드
[ ] G-3  채용 공고 실 리스트 컴포넌트
[ ] G-4  Hero 카피 위계 + 팀 일러스트

[P1 - Notices]
[ ] H-1  카테고리 필터 + 리스트 + slug 상세 + 빈 상태

[P2 - 일관성]
[ ] I-1  섹션 패딩 토큰
[ ] I-2  타이포 스케일 토큰
[ ] I-3  버튼 사이즈 토큰
[ ] I-4  아이콘 stroke 1.5
[ ] I-5  focus-visible 토큰
[ ] I-6  이미지 모서리 12px
[ ] I-7  모션 토큰

================================================================
PART 3. 우선순위 테이블
================================================================

ID    | Priority | 영역      | 한 줄
------+----------+-----------+----------------------------------------------
A-1   | P0       | 전체      | 숫자 워터마크 수직 분리 (nowrap)
A-2   | P0       | 전체      | 카운터 회색 잔존 + 줄바꿈
A-3   | P0       | 전체      | 다크 위 텍스트 대비
A-4   | P0       | 전체      | 헤더 sticky + blur
A-5   | P0       | 라우팅    | /cases/[slug], /contact 404
A-6   | P0       | 전체      | body 색 토큰 미반영
B-1   | P1       | Home      | Hero mesh + 빌딩 SVG
B-2   | P1       | Home      | Floating glass card
B-3   | P1       | Home      | BUSINESS 16:9 비주얼
B-4   | P1       | Home      | CASES 배지/4:5/ken-burns
B-5   | P1       | 전체      | ContactForm 9필드
C-1   | P1       | About     | 탭 pill
C-2   | P1       | About     | 세 가지 가치 아이콘 배지
C-3   | P1       | About     | 다섯 가지 차이 sticky
C-4   | P1       | About     | history sticky 타임라인
C-5   | P1       | About     | Kakao Map
D-1   | P1       | Business  | alternating 레이아웃
D-2   | P1       | Business  | 3 metrics
D-3   | P1       | Business  | 갤러리 + FAQ + PROCESS connector
E-1   | P1       | Cases     | SVG 한국 지도
E-2   | P1       | Cases     | sticky 필터 바
E-3   | P1       | Cases     | /cases/[slug] 스켈레톤
E-4   | P1       | Cases     | 카드 hover ken-burns
F-1   | P1       | Licenses  | 3 KPI 대시보드
F-2   | P1       | Licenses  | 도넛 차트
F-3   | P1       | Licenses  | 라이트박스
F-4   | P1       | Licenses  | 다크 섹션 토큰
G-1   | P1       | Careers   | BENEFITS 8
G-2   | P1       | Careers   | 인재풀 모달
G-3   | P1       | Careers   | 채용 공고 리스트
G-4   | P1       | Careers   | Hero 위계
H-1   | P1       | Notices   | 카테고리/리스트/슬러그
I-1   | P2       | 일관성    | 섹션 패딩 토큰
I-2   | P2       | 일관성    | 타이포 스케일
I-3   | P2       | 일관성    | 버튼 사이즈
I-4   | P2       | 일관성    | 아이콘 stroke 1.5
I-5   | P2       | 일관성    | focus-visible
I-6   | P2       | 일관성    | 이미지 모서리 12
I-7   | P2       | 일관성    | 모션 토큰

================================================================
PART 4. 핵심 요약 (Claude Code 작업 가이드)
================================================================

1) P0(A-1 ~ A-6) 먼저 처리. 6개만 끝내도 체감 품질 대폭 상승.
2) 다음 P1에서는 Home(B) → About(C) → Business(D) 순서 권장.
3) Cases / Licenses는 시각화 컴포넌트(지도, 차트, 라이트박스)가 무거우므로
   B/C/D 작업 사이에 분리 PR로 진행.
4) Notices(H)는 단일 라우트라 빠르므로 P1 마무리 시점에 끼워서 처리.
5) P2(I)는 토큰만 정리하면 끝. K 섹션의 globals.css를 통째로 교체하는 게 최단 루트.
6) Stripe / Linear / Apple hero를 B-1, B-2 작업 시 레퍼런스로 사용.

================================================================
END OF DOCUMENT
================================================================
